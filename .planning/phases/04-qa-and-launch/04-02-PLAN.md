---
phase: 04-qa-and-launch
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements:
  - DSGN-01
  - DSGN-02
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "All 4 pages display correctly at 375px width in browser devtools"
    - "No horizontal scrollbar on any page at 375px"
    - "Navigation collapses to hamburger menu on mobile"
    - "Site is deployed and live at https://rjm328.github.io/"
    - "Live URL returns HTTP 200 and renders all pages"
    - "User has reviewed and approved the QA results"
  artifacts: []
  key_links:
    - from: "local _site/ build"
      to: "https://rjm328.github.io/"
      via: "quarto publish gh-pages"
      pattern: "rjm328.github.io"
---

<objective>
Verify mobile rendering on all 4 pages, deploy the final site, confirm the live URL works, and get user sign-off.

Purpose: Complete the visual QA and deployment verification that cannot be fully automated -- mobile layout, design intent, and live site confirmation. User sign-off marks the site as launched.
Output: Deployed, verified site at https://rjm328.github.io/ with user approval.
</objective>

<execution_context>
@/Users/rmcgrath/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rmcgrath/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qa-and-launch/04-01-SUMMARY.md
@.planning/phases/04-qa-and-launch/04-RESEARCH.md

Depends on Plan 01 completing: all DOIs verified, links audited, CV cross-checked, data_url fields finalized.

Mobile QA checklist from research:
| Page | Check |
|------|-------|
| Home | Profile photo above bio text (not side-by-side) at 375px |
| Home | Sidebar links (email, scholar, ORCID, GitHub) visible and tappable |
| Publications | Entries readable; DOI buttons don't overflow |
| Publications | Working papers gold accent bar visible |
| CV | PDF download button visible and tappable |
| CV | Two-column layout collapses gracefully |
| Contact | Profile buttons wrap cleanly |
| All pages | Navigation bar collapses to hamburger menu |
| All pages | No horizontal scrollbar |

INFR-02 (custom domain with HTTPS) is DEFERRED per CONTEXT.md -- domain not purchased. QA runs against rjm328.github.io only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile rendering verification and final deployment</name>
  <files></files>
  <action>
**1. Fresh build:**
Run `quarto render --no-cache` to ensure _site/ is current with any publications.yml changes from Plan 01.

**2. Mobile rendering checks (375px devtools simulation):**
Per user decision: browser devtools at 375px width is acceptable, no real-device testing required. Check all 4 pages.

Since Claude cannot open a browser, perform the closest automated equivalent:
- Verify the custom.scss mobile media query is present: `@media (max-width: 768px)` with `.about-image { margin-top: 1.5rem; }`
- Verify `.pub-buttons` has `flex-wrap: wrap` in custom.scss (prevents button overflow on mobile)
- Verify `.quarto-container` max-width is 720px (fits in 375px viewport with padding)
- Verify Bootstrap responsive navbar classes are present in rendered HTML (cosmo theme provides this)
- Check `_site/index.html` for responsive image markup
- Check `_site/publications.html` for no fixed-width elements that would cause horizontal scroll
- Check `_site/cv.html` for Bootstrap grid classes that collapse on mobile
- Check `_site/contact.html` for flex-wrap on button groups

Document each check result in a mobile QA table for the user to review at the checkpoint.

**3. Deploy to GitHub Pages:**
Run `quarto publish gh-pages --no-prompt` to deploy the site. If this fails with the known multiple-publish error, use the git worktree workaround established in Phase 2.

**4. Verify live site:**
After deployment, verify `https://rjm328.github.io/` returns HTTP 200:
```
curl -s -o /dev/null -w "%{http_code}" https://rjm328.github.io/
```
Also verify each page:
```
curl -s -o /dev/null -w "%{http_code}" https://rjm328.github.io/publications.html
curl -s -o /dev/null -w "%{http_code}" https://rjm328.github.io/cv.html
curl -s -o /dev/null -w "%{http_code}" https://rjm328.github.io/contact.html
```
All should return 200.

**5. Prepare QA summary for user checkpoint:**
Compile the complete QA results from Plan 01 (DOI verification, link audit, CV cross-check) and this plan (mobile checks, deployment status, live URL verification) into a single summary table. Present this at the checkpoint for user review.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://rjm328.github.io/ | grep -q "200" && echo "PASS: Live site returns 200" || echo "FAIL: Live site not returning 200"</automated>
  </verify>
  <done>Site deployed to GitHub Pages. All 4 pages return HTTP 200 at rjm328.github.io. Mobile CSS checks documented. QA summary compiled for user review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User QA sign-off and launch approval</name>
  <action>
Present the complete QA summary to the user for review and approval. All automated checks have been run:
- 22 DOIs verified against CrossRef API
- All internal and external links audited
- CV publication counts cross-checked against publications.yml
- Data repository URLs searched and data_url fields updated (or confirmed empty)
- Mobile CSS verified for 375px rendering
- Site deployed to https://rjm328.github.io/
- All 4 pages returning HTTP 200

INFR-02 (custom domain with HTTPS) is deferred -- domain not yet purchased. This does not block launch.

User verification steps:
1. Open https://rjm328.github.io/ in your browser -- confirm the home page loads with your photo, name, and research interests
2. Click through all 4 navigation links (Home, Publications, CV, Contact) -- confirm each page loads
3. On Publications page: click 2-3 DOI links -- confirm they open the correct paper
4. On CV page: click the PDF download button -- confirm the PDF downloads
5. On Contact page: verify your email, ORCID, Google Scholar, GitHub, LinkedIn links work
6. Open browser devtools (F12), set viewport to 375px width, check each page:
   - Home: photo should be above text, not beside it
   - Publications: entries readable, no horizontal scroll
   - CV: content readable, PDF button visible
   - Contact: buttons wrap cleanly
7. Review the QA summary table -- confirm you are satisfied with the results

The site is "launched" when you approve. You share the link informally as needed (no formal announcement per your decision).
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>User has reviewed all QA results, verified the live site, and approved. Phase 4 complete -- site is launched.</done>
</task>

</tasks>

<verification>
1. `quarto render --no-cache` succeeds
2. Mobile CSS properties confirmed present in custom.scss and rendered HTML
3. `quarto publish gh-pages` succeeds (site deployed)
4. All 4 live URLs return HTTP 200
5. User approves QA at checkpoint
</verification>

<success_criteria>
- Site deployed and live at https://rjm328.github.io/
- All 4 pages return HTTP 200 from live URL
- Mobile CSS checks documented and reasonable
- User has reviewed all QA results and approved
- Phase 4 complete -- site is launched
</success_criteria>

<output>
After completion, create `.planning/phases/04-qa-and-launch/04-02-SUMMARY.md`
</output>
