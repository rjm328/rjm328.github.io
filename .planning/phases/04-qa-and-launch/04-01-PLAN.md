---
phase: 04-qa-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - publications.yml
autonomous: true
requirements:
  - PUBL-02
  - PUBL-04
  - IDEN-01
  - IDEN-02
  - IDEN-03
  - PUBL-01
  - PUBL-03
  - CV-01
  - CV-02
  - CONT-01
  - CONT-02
  - DSGN-03
  - INFR-01

must_haves:
  truths:
    - "All 22 DOI links resolve to the correct paper title via CrossRef API"
    - "data_url fields in publications.yml are populated for any papers with public Dataverse/ICPSR deposits, or confirmed empty if none found"
    - "CV page publication counts (22 articles, 1 chapter, 3 working papers) match publications.yml entry counts"
    - "All internal navigation links resolve (Home, Publications, CV, Contact)"
    - "All external profile links on Contact page resolve (ORCID, Google Scholar, GitHub, LinkedIn)"
    - "CV PDF file exists and is downloadable"
    - "Site builds without errors via quarto render"
  artifacts:
    - path: "publications.yml"
      provides: "Publication data with verified DOIs and data_url fields"
      contains: "data_url"
  key_links:
    - from: "publications.yml DOI fields"
      to: "CrossRef API"
      via: "api.crossref.org/works/{doi} title match"
      pattern: "doi.*https://doi.org"
    - from: "cv.qmd count statements"
      to: "publications.yml entry counts by type"
      via: "manual count comparison"
      pattern: "22 peer-reviewed articles"
---

<objective>
Verify all links, DOIs, cross-references, and data integrity across the site before launch.

Purpose: Confirm that everything built in Phases 1-3 is correct -- no broken links, no wrong DOIs, no count mismatches, and data_url fields are populated where public deposits exist.
Output: Verified site with all automated checks passing, data_url fields updated if deposits found, QA verification log.
</objective>

<execution_context>
@/Users/rmcgrath/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rmcgrath/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qa-and-launch/04-RESEARCH.md

Key facts from research (executor does NOT need to re-research):
- All 22 DOIs already verified PASS against CrossRef API during research phase
- No Dataverse/ICPSR data repository URLs found via public web search
- CV counts (22/1/3) match publications.yml counts
- All internal navigation links verified correct in rendered HTML
- All external profile links catalogued and verified present

The executor re-runs DOI verification as a scripted confirmation (not re-research).
Data_url lookup is a best-effort search using public APIs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOI verification, link audit, and CV cross-check</name>
  <files>publications.yml</files>
  <action>
Run three verification checks and produce a combined QA log:

**1. DOI Verification (all 22):**
For each DOI in publications.yml, call CrossRef API and confirm the returned title matches the expected title. Use the polite pool pattern:
```
curl -s "https://api.crossref.org/works/{doi_suffix}?mailto=rmcgrat2@gmu.edu"
```
Extract `message.title[0]` from the JSON response. Compare against the `title` field in publications.yml. Accept minor differences (capitalization, "U.S." vs "US"). Flag any DOI where the title does not match.

Per user decision: verify ALL 22, not a spot-check. Per Phase 2 lesson: HTTP 200 is NOT sufficient -- title matching via CrossRef is required.

Write results as a verification table (DOI | Expected Title | CrossRef Title | PASS/FAIL).

**2. Link Audit:**
Run `quarto render --no-cache` to produce fresh `_site/` output. Then verify:
- All 4 internal nav links resolve: index.html, publications.html, cv.html, contact.html exist in _site/
- CV PDF exists: `_site/RJM-CV-v.oct25.pdf` exists and is > 0 bytes
- External links on contact page are present in rendered HTML: grep for the 4 profile URLs (ORCID, Google Scholar, GitHub, LinkedIn) and mailto link in `_site/contact.html`
- All 22 DOI hrefs present in `_site/publications.html`
- Homepage has profile photo: grep for profile.jpg in `_site/index.html`
- Homepage research interests: count sentences in the research interests paragraph (should be 3-5)
- Publications page has 3 sections (peer-reviewed, book chapters, working papers)
- Navigation bar has exactly 4 items (Home, Publications, CV, Contact) -- verify in `_site/index.html`
- pub-summary class appears 26 times in `_site/publications.html` (one per entry)

Log results as a checklist (PASS/FAIL per item).

**3. CV-Publications Cross-Check:**
Count entries in publications.yml by type: articles (type: article), chapters (type: chapter), working papers (type: working_paper). Compare against the count statements in cv.qmd ("22 peer-reviewed articles", "1 book chapter", "3 working papers and works in progress"). Log PASS if counts match, FAIL with details if they differ.

Read the CV PDF at `RJM-CV-v.oct25.pdf` and compare the publication list against publications.yml entries. Note: Book reviews appear in the CV PDF but are intentionally excluded from publications.yml (by design from Phase 3 decision). Working papers in the PDF should match the 3 working-paper entries in publications.yml. Log any discrepancies.

Output all results as a QA log printed to the terminal. If ALL checks pass, no file changes needed except the data_url updates from Task 2.
  </action>
  <verify>
    <automated>quarto render --no-cache 2>&1 | grep -iE "error" | head -5; echo "Build check complete"</automated>
  </verify>
  <done>All 22 DOIs confirmed matching via CrossRef API. All internal links resolve. All external profile links present in HTML. CV counts match publications.yml counts (22/1/3). QA log printed to terminal.</done>
</task>

<task type="auto">
  <name>Task 2: Data repository URL lookup and publications.yml update</name>
  <files>publications.yml</files>
  <action>
Attempt to find Dataverse/ICPSR replication data URLs for publications in publications.yml.

**Lookup strategy (from research):**

1. Search Harvard Dataverse LSQ collection for McGrath papers:
   - Visit `https://dataverse.harvard.edu/dataverse/lsq` and search for "McGrath"
   - Also try general Harvard Dataverse search: `https://dataverse.harvard.edu/` search "McGrath" + key terms

2. Search OpenICPSR:
   - `https://www.openicpsr.org/openicpsr/search/` search for "McGrath" and paper titles

3. For recent JPART papers (2020, 2023), try Oxford Academic supplementary data links:
   - These are the most likely to have deposits per journal policy

4. For recent papers (2024, 2025), try journal article pages for data availability statements.

**Papers most likely to have deposits (prioritize these):**
- Boushey & McGrath 2023, "Rulemaking Speed" (JPART) -- 250K+ rules dataset
- Boushey & McGrath 2020, "Does Partisan Conflict" (JPART) -- 150K+ rules dataset
- McGrath & Ryan 2019, "Party Effects" (LSQ) -- LSQ Dataverse active by 2019
- MacDonald & McGrath 2019, "A Race for the Regs" (LSQ)
- Chen, Franko & McGrath 2025, "Occupational Licensing" (JPAM)

**If a valid data repository URL is found:** Update the `data_url` field for that entry in publications.yml with the full URL. This will cause the [Data] button to appear on the publications page.

**If no URL is found after searching:** Leave `data_url: ""` -- do NOT fabricate URLs. The [Data] button correctly does not appear when data_url is empty. Per user decision from CONTEXT.md: "Papers without associated datasets keep data_url: ''"

**Important:** Research already found no publicly indexed URLs via web search. This task is a best-effort confirmation using direct API/collection searches. It is acceptable to conclude with all data_url fields remaining empty if no deposits are found.

After any publications.yml changes, run `quarto render --no-cache` to confirm the site still builds and any new [Data] buttons render correctly.
  </action>
  <verify>
    <automated>python3 -c "import yaml; data=yaml.safe_load(open('publications.yml')); print(f'Total entries: {len(data)}'); print(f'With data_url: {sum(1 for d in data if d.get(\"data_url\",\"\"))}')"</automated>
  </verify>
  <done>All 26 publications.yml entries checked for data repository URLs. data_url fields populated where public deposits were found, or confirmed empty where none exist. Site builds cleanly after any changes.</done>
</task>

</tasks>

<verification>
1. `quarto render --no-cache` completes without errors
2. All 22 DOIs return matching titles from CrossRef API
3. CV publication counts match publications.yml entry counts by type
4. All 4 internal navigation pages exist in _site/
5. CV PDF exists and is downloadable
6. External profile links present in contact.html
7. publications.yml data_url fields are populated where deposits exist, empty where not
</verification>

<success_criteria>
- Zero DOI verification failures (22/22 PASS)
- Zero broken internal links (4/4 pages render)
- CV PDF present and linked
- CV counts match YAML counts (22 articles, 1 chapter, 3 working papers)
- All contact page external links present in HTML
- data_url fields reflect best-effort lookup results
- Site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-qa-and-launch/04-01-SUMMARY.md`
</output>
