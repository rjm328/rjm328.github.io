---
phase: 08-data-repository-links
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - _site/publications.html
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "Publications page renders correctly with all [Data] buttons working"
    - "Live site at rjm328.github.io shows updated publications page"
  artifacts:
    - path: "_site/publications.html"
      provides: "Rendered publications page with all Data buttons"
  key_links:
    - from: "publications.qmd"
      to: "_site/publications.html"
      via: "quarto render"
      pattern: "btn.*Data"
    - from: "_site/"
      to: "https://rjm328.github.io/"
      via: "git worktree gh-pages deployment"
---

<objective>
Build, verify, and deploy the updated publications page to GitHub Pages.

Purpose: After data repository search and user approval, render the site and deploy so the live site reflects any new [Data] buttons.

Output: Live site at https://rjm328.github.io/publications.html with all data links working.
</objective>

<execution_context>
@/Users/rmcgrath/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rmcgrath/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-repository-links/08-01-SUMMARY.md
@publications.yml
@publications.qmd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build site and verify publications page</name>
  <files>_site/publications.html</files>
  <action>
1. Run `quarto render` to build the full site
2. Verify the build succeeds without errors
3. Check _site/publications.html exists and contains all expected [Data] buttons:
   - Count the number of Data buttons in the rendered HTML
   - Verify each data_url from publications.yml appears as a button href in the rendered page
   - Confirm the 3 known deposits (occupational-licensing, rulemaking-speed, race-regs) still render correctly
   - If Plan 01 found additional deposits, confirm those render too
4. Run the smoke test: `bash scripts/check-render.sh` (if it exists)
  </action>
  <verify>
    <automated>quarto render && grep -c 'Data</a>' _site/publications.html</automated>
  </verify>
  <done>Site builds cleanly and publications.html contains the correct number of [Data] buttons matching all non-empty data_url fields in publications.yml</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to GitHub Pages via git worktree</name>
  <files>_site/publications.html</files>
  <action>
Deploy the rendered site to GitHub Pages using the git worktree method (quarto publish --no-prompt has a known issue with "Multiple previous publishes exist").

Steps:
1. Ensure the gh-pages worktree exists: `git worktree list` to check; if not present, add it: `git worktree add _site-deploy gh-pages`
2. Copy rendered _site/ contents to the worktree directory
3. In the worktree, add all files, commit with message "deploy: update data repository links", and push to gh-pages
4. Verify deployment: use `curl -s -o /dev/null -w '%{http_code}' https://rjm328.github.io/publications.html` — expect 200
5. Wait 30 seconds for GitHub Pages to propagate, then verify again if needed

Note: The exact worktree deployment method may vary — check how prior phases deployed (Phase 7 used git worktree). Adapt as needed based on the current state of worktrees (`git worktree list`).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://rjm328.github.io/publications.html</automated>
  </verify>
  <done>Live site at https://rjm328.github.io/publications.html returns HTTP 200 and displays all [Data] buttons correctly</done>
</task>

</tasks>

<verification>
- `quarto render` completes without errors
- _site/publications.html contains all expected [Data] buttons
- https://rjm328.github.io/publications.html returns HTTP 200
- All data_url links in the live page resolve to valid repository pages
</verification>

<success_criteria>
- Site builds cleanly
- Publications page renders with correct [Data] buttons
- Live deployment at rjm328.github.io shows updated page
- All data repository URLs are clickable and resolve
</success_criteria>

<output>
After completion, create `.planning/phases/08-data-repository-links/08-02-SUMMARY.md`
</output>
